{application, pts,
   [
      {description, "process term storage"},
      {vsn, "2.0.0"},
      {modules, [
         pts,
         pns,
         pts_cache,
         pts_entity_sup,
         pts_ns,
         pts_ns_sup,
         pts_sup,
         pts_app
      ]},
      {registered, []},
      {applications, [
         kernel,
         stdlib,
         pns
      ]},
      {mod, {pts_app, []}},
      {env, [
      ]}
   ]
}.