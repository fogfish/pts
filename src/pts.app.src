{application, pts,
   [
      {description, "process term storage"},
      {vsn, "3.2.1"},
      {modules,    []},
      {registered, []},
      {applications, [
         kernel,
         stdlib,
         pipe,
         pns
      ]},
      {mod, {pts_app, []}},
      {env, [
      ]}
   ]
}.
