{application, pts,
   [
      {description, "process term storage"},
      {vsn, "3.1.0"},
      {modules,    []},
      {registered, []},
      {applications, [
         kernel,
         stdlib,
         pipe,
         pns
      ]},
      {mod, {pts_app, []}},
      {env, [
      ]}
   ]
}.
