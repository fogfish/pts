{application, pts,
   [
      {description, "process term storage"},
      {vsn, "0.6.0"},
      {modules, [
         pts,
         pns,
         pts_cache,
         pts_cache_sup,
         pts_sup,
         pts_app
      ]},
      {registered, []},
      {applications, [
         kernel,
         stdlib
      ]},
      {mod, {pts_app, []}},
      {env, [
      ]}
   ]
}.